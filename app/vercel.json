{
  "version": 1,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/app/.*", "dest": "/index.html" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
